<h2>Having a party and need to pick the perfect game?</h2>

<p>Enter the number of guests, desired game length, and/or the game type below, and we’ll search through our database for the ideal game for your get together!</p>

<%= form_tag(advanced_search_path, :method => "get") do %>
	<p><%= label_tag(:players, "Number of players:") %>
  <%= number_field_tag(:players) %></p>
  <p><%= label_tag(:game_time, "Approximate game time (in minutes)") %>
  <%= number_field_tag(:game_time) %></p>
  <p><label for="mechanics">Type</label>
  <%= select_tag "mechanics", options_from_collection_for_select(@mechanics, "trait", "trait"), {include_blank: true} %></p>
  <%= submit_tag("Search") %>
<% end %>